use super::endpoints::EndpointWatch;
pub struct SearchPlaylistVideo{
    pub title: String,
    pub id: String,
    pub length_seconds: i32,
    pub endpoint: EndpointWatch,
}

impl SearchPlaylistVideo {
    
}